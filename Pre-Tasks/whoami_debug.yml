- name: PRE | DEBUG | var print test
  tags: debug
  debug:
    msg: "{{ Setup }}"

- name: PRE | DEBUG | print name
  tags: debug
  debug:
    msg: "{{ username }}"

- name: PRE | DEBUG | print dir
  tags: debug
  debug:
    msg: "{{ user_dir}}"

- name: PRE | DEBUG | print ditro
  tags: debug
  debug:
    msg: "{{ distribution }}"

- name: PRE | DEBUG | print Hostname
  tags: debug
  debug:
    msg: "{{ hostname }}"
