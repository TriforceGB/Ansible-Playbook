- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: Install Packages
    community.general.pacman:
     name: 
      - ansible
      - fzf
      - less
      - otf-firamono-nerd
      # - PulseAudio Later
      - stow
      - tree
      # - yay Later
      - zoxide
      - zsh
      # - zen Later